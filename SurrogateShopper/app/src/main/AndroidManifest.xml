<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:banner="@mipmap/ic_launcher_foreground"
        android:logo="@mipmap/ic_launcher_foreground"

        android:supportsRtl="true"
        android:theme="@style/Theme.Surrogateshopper"
        android:icon="@mipmap/ic_launcher"
        tools:targetApi="31">


        <!-- Launcher Activity with Splash Theme -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Surrogateshopper.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".ShopperProfileActivity" />
        <activity android:name=".ShopperRequestItemsActivity" />
        <activity android:name=".VolunteerProfileActivity" />
        <activity android:name=".VolunteerRequestDetailsActivity" />
        <activity android:name=".VolunteerViewRequestsActivity" />
        <activity android:name=".ShopperSendMessage" />

    </application>

</manifest>


